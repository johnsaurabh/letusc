#include<stdio.h>
int main()
{
	int num,a,b,c,d,e,sum;
	printf("enter the digits of numbers from lft to right");
		num=e+d*10+c*100+b*1000+a*10000;
	scanf("%d",&num);

	 printf ("the number is%d",num);
	 	sum= a+b+c+d+e;
	printf ("the sum of the digits is %d",sum);
	return 0;
}
