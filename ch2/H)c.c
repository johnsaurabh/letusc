#include<math.h>
#include<stdio.h>
int main()
{
	float a,b,c;
	 float s,area;
	printf("Ener the sides of the trianle\n\n\n");
	scanf("%d%d%d",&a,&b,&c);
	s= (a+b+c)/2;
	printf ("The semiperimeter is %f",s);
		area = pow((1/2).0,(s-a)*(s-b)*(s-c)*s.0);
	
	printf ("The area of the triangle is %f",area);
	return 0;
	}

